[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: end-to-end replay and observability tests
    websocket: socket streaming validation tests
    smoke: fast replay smoke tests
addopts = -q