# Transport System Dashboard

This workspace contains a React + Vite transport operations dashboard for lorry dispatch, fleet monitoring, route activity, and shipment tracking.

## Working Notes

- Keep changes focused on the transport dashboard experience.
- Prefer responsive, single-page updates unless the user asks for more screens.
- Validate changes with `npm run build` before finishing.
